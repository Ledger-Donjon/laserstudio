{
  "$id": "focussearch.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Focus search settings",
  "type": "object",
  "properties": {
    "span": {
      "type": "number",
      "default": 4000.0,
      "exclusiveMinimum": 0.0,
      "suffix": "um",
      "description": "The Z span, in micrometers, explored during the focus search."
    },
    "steps": {
      "type": "integer",
      "default": 20,
      "minimum": 2,
      "description": "Number of Z steps sampled across the span."
    },
    "averaging": {
      "type": "integer",
      "default": 5,
      "minimum": 1,
      "description": "Number of frames averaged at each step."
    },
    "multi_peaks": {
      "type": "boolean",
      "default": true,
      "description": "If true, keep several sharpness peaks instead of only the best one."
    },
    "best_is_highest_z": {
      "type": "boolean",
      "default": true,
      "description": "When peaks are equivalent, prefer the one at the highest Z."
    }
  }
}
