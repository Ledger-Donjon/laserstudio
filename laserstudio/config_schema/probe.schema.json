{
  "$id": "probe.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Probe",
  "type": "object",
  "allOf": [
    {
      "$ref": "instrument.schema.json"
    }
  ],
  "description": "A Probe device.",
  "properties": {
    "offset_pos": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "description": "The relative position from the center of the camera without distortion, in pixels.",
      "items": {
        "type": "number",
        "suffix": "px"
      }
    },
    "spot_size_um": {
      "type": "number",
      "default": 10.0,
      "exclusiveMinimum": 0.0,
      "suffix": "um",
      "description": "The diameter of the probe spot, in micrometers. (Legacy alias: 'spot_size'.)"
    }
  }
}
